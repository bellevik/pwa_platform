services:
  caddy:
    image: caddy:2.8.4
    restart: unless-stopped
    depends_on:
      megafactory-mobile-backend:
        condition: service_started
      shopping-list-backend:
        condition: service_started
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./caddy/Caddyfile:/etc/caddy/Caddyfile:ro
      - ./caddy/certs:/etc/caddy/certs:ro
      - ../shell/dist:/srv/shell:ro
      - ../generated:/srv/generated:ro
      - ../apps:/srv/apps:ro

  shopping-list-backend:
    image: node:22-alpine
    working_dir: /workspace
    restart: unless-stopped
    command: >-
      sh -lc "corepack enable && pnpm --filter @pwa-platform/shopping-list-backend start"
    environment:
      HOST: 0.0.0.0
      NODE_OPTIONS: --experimental-sqlite
      PORT: 4300
    volumes:
      - ..:/workspace

  megafactory-mobile-backend:
    image: node:22-alpine
    working_dir: /workspace
    restart: unless-stopped
    command: >-
      sh -lc "corepack enable && pnpm --filter @pwa-platform/megafactory-mobile-backend start"
    environment:
      HOST: 0.0.0.0
      NODE_OPTIONS: --experimental-sqlite
      PORT: 4310
    volumes:
      - ..:/workspace
